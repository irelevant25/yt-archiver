# Copy to .env next to docker-compose.yml and change the values before the first start.

# Password of the bundled PostgreSQL database (used by both the app and the postgres service).
# Generate one with: openssl rand -hex 24
YTA_DB_PASSWORD=change-me
